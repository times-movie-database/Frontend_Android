<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moviezzzz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tmdblogolarge"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tmdblogolarge"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moviezzzz">
        <activity
            android:name=".SplashScreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ErrorHandled"
            android:exported="true">

        </activity>
        <activity
            android:name=".Movie_Details"
            android:exported="true" />
        <activity
            android:name=".AddEditMovie"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".HomeActivity">
        </activity>
        <activity
            android:name=".SearchActivity"
            android:parentActivityName=".HomeActivity" />
    </application>

</manifest>